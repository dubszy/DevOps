group 'com.mwaltman.devops'
version '0.0.1'

dependencies {

    String dwAuthVersion = '1.3.2'
    String junitVersion = '4.12'

    compile group: 'io.dropwizard', name: 'dropwizard-auth', version: dwAuthVersion

    testCompile group: 'junit', name: 'junit', version: junitVersion
}
